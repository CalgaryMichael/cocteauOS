#!/bin/sh

# Builds the kernel and launches it using qemu

set -e

DIRNAME=$(dirname $0)

. $DIRNAME/iso
qemu-system-$HOST_ARCH -cdrom $DIRNAME/../build/usr/boot/grub/cocteauOS.iso

#. $DIRNAME/build
#qemu-system-$HOST_ARCH -kernel $DIRNAME/../libs/kernel/cocteauOS.kernel

